# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://milosen.github.io"
title: "Nikola Milosevic"

# Build settings
markdown: kramdown
plugins:
  - jekyll-email-protect
  - jekyll-feed

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
# exclude:
#   - Gemfile
#   - Gemfile.lock
#   - node_modules
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/

# timezone: Asia/Shanghai

collections:
  publications:
    output: false
  news:
    output: false
  more:
    output: false

# Internationalization.
# `default_lang` is the language assumed for any page that does not declare
# `lang` in its front matter, and the language served at the site root.
default_lang: en
languages:
  - code: en
    name: English
    # Path prefix for this language's pages. Empty means "served at the root".
    prefix: ""
  - code: de
    name: Deutsch
    prefix: "/de"

defaults:
  - scope:
      path: ""
    values:
      lang: en
  - scope:
      path: "de"
    values:
      lang: de
  - scope:
      path: "_posts"
      type: "posts"
    values:
      lang: en
