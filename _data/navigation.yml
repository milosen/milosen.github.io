pages:
# NOTE: `name` field must match the `navbar_title` field in the page's front matter to ensure the correct page is highlighted in the navbar.
#       For example, the `navbar_title` field in `More.html` is "More" and the `name` field here should also be "More"
- name: Home
  url: /
- name: Publications
  url: /publications
- name: More
  url: /more
